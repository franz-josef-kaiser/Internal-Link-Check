// Just here in case I decide to add AJAX driven settings directly to the meta box
module.exports = function( grunt ) {
	"use strict";

	grunt.initConfig( {
		pkg : grunt.file.readJSON( "package.json" )
	} );
};